<{{ tag }} id="comment-{{ id|absint }}" {{ class_attr }}>
	<article>
		<footer class="comment-meta">
			{{ author_link }}

			<a href="{{ permalink_url|esc_url }}" class="comment-permalink">
				#
			</a>
			<time datetime="{{ machine_date|esc_attr }}" class="comment-datetime">
				{{ date }} {{ time }}
			</time>
		</footer>

		{{ text }}

	</article>

	{{ reply_link }}
